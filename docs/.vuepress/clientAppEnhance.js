import { defineClientAppEnhance } from '@vuepress/client';
// import ClipInfo from './components/ClipInfo';

export default defineClientAppEnhance(({ app, router, siteData }) => {
    // app.component('ClipInfo', ClipInfo);
});